# Athena Cheat Sheet

Use Athena for SQL analysis of CloudTrail logs in S3. Partition by date/account/Region, use encrypted result locations, select only needed columns, and constrain time ranges to control cost. Preserve query text and execution IDs in the incident record.

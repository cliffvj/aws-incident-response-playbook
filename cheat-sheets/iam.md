# IAM Cheat Sheet

Investigate users, roles, session issuers, access keys, trust policies, permission boundaries, inline and managed policies, groups, MFA, login profiles, and service-specific credentials. Search for `CreateUser`, `CreateAccessKey`, `CreateLoginProfile`, `Attach*Policy`, `Put*Policy`, `UpdateAssumeRolePolicy`, `PassRole`, and policy-version changes.

# AWS Config Cheat Sheet

Use AWS Config to record resource configuration, evaluate rules, and inspect timelines. Common study rules include `required-tags`, `cloudtrail-enabled`, `restricted-ssh`, `restricted-common-ports`, and EC2 detailed-monitoring controls. Remediation must be tested, scoped, and idempotent.

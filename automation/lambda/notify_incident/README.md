# `notify_incident`

See the [Lambda action catalog](../README.md), [event contract](../../docs/event-contract.md), and matching sample event under [`automation/samples/`](../../samples/README.md). The handler is `app.handler`. Mutating actions default to dry-run behavior.

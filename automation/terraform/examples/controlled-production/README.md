# controlled-production example

This example composes the reusable platform module for a **controlled-production** environment. Review variables and account scope before `terraform plan`. It is an adaptation template, not a guarantee that a production account should use these defaults.
